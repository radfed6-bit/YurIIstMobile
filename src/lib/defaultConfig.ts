// Значения по умолчанию, перенесённые из исходного .env серверной части.
// Это приложение личное и не публикуется — ключ "зашит" внутрь по прямой
// просьбе владельца проекта (см. переписку). Он используется как дефолт,
// если пользователь не переопределил его в Настройках (переопределение
// хранится локально на устройстве, поверх этих значений).
//
// ВАЖНО: любой, кто получит .apk-файл, сможет извлечь этот ключ обратной
// разработкой (это не секрет сервера — секрет клиента). Приемлемо только
// для строго личного использования, не для публикации в Google Play/APKPure
// и т.п. Если понадобится отозвать доступ — смените ключ в кабинете
// opencode.ai/zen и пересоберите приложение.

export interface LLMConfig {
  apiKey: string;
  baseUrl: string;
  model: string;
}

export const DEFAULT_LLM_CONFIG: LLMConfig = {
  apiKey: "example",
  baseUrl: "https://opencode.ai/zen/v1",
  model: "deepseek-v4-flash-free",
};

export const DEFAULT_WEB_SEARCH_MCP_URL = "https://search.parallel.ai/mcp";
